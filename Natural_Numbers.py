from ast import Num
import numbers


num = int(input("Enter a number:"))

if num < 0:
    print("Enter a positive number")
Else:
    sum = 0
    # use while lopp to iterate until zero
    while(num>o):
        sum += num
        num -=1
        print("the sum is",sum)
